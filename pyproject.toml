[project]
name = "tabpfn-cookbook"
version = "0.0.0"
description = "Source notebooks and tooling for TabPFN cookbooks."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
]

[tool.uv]
package = false
